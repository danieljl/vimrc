let g:session_autosave = 'yes'
let g:session_autoload = 'no'

if has("win16") || has("win32")
    set gfn=Fantasque_Sans_Mono:h11:cANSI
endif

if has("gui_running")
    set lines=50 columns=100
endif
